# 视频播放问题排查指南

## 🔍 问题排查

### 问题1：视频不显示

**可能原因**：
1. 视频文件不存在
2. 视频路径错误
3. 视频文件损坏

**排查步骤**：
1. 检查文件是否存在：
   - 路径：`media/video/performance/百年文脉传承人谈起源.mp4`
   - 文件名是否正确（注意：没有"视频"前缀）

2. 在浏览器中直接访问视频URL测试：
   ```
   http://localhost/media/video/performance/百年文脉传承人谈起源.mp4
   ```

3. 检查浏览器控制台是否有错误：
   - 按F12打开开发者工具
   - 查看Console标签页
   - 查找404错误或视频加载错误

### 问题2：视频不自动播放

**可能原因**：
1. 浏览器自动播放策略限制
2. 用户未与页面交互
3. 视频文件未加载完成
4. JavaScript错误

**排查步骤**：
1. 检查浏览器控制台日志：
   - 应该看到"视频已加载，时长: X 秒"
   - 如果看到"自动播放被阻止"消息，需要用户先点击页面

2. 确认用户交互：
   - 在页面任意位置点击一次
   - 然后滚动到视频位置

3. 手动测试视频播放：
   - 点击视频播放按钮
   - 确认视频可以正常播放

### 问题3：视频比例不对

**可能原因**：
1. CSS样式问题
2. 视频源文件比例不是16:9

**解决方案**：
1. 修改CSS中的aspect-ratio值：
   ```css
   .video-wrapper video {
       aspect-ratio: 4 / 3; /* 如果是4:3视频 */
   }
   ```

2. 或移除aspect-ratio，使用自动高度：
   ```css
   .video-wrapper video {
       width: 100%;
       height: auto;
   }
   ```

## 📋 正确的文件结构

```
南曲/
├── media/
│   └── video/
│       └── performance/
│           └── 百年文脉传承人谈起源.mp4  ← 视频文件在这里
├── images/
│   └── modules/
│       └── module1/
│           └── 视频封面-传承人谈起源.jpg  ← 封面图在这里
└── module1/
    └── page1.html
```

## 🎯 当前代码配置

### HTML
```html
<video id="inheritorVideo" preload="auto" controls poster="../images/modules/module1/视频封面-传承人谈起源.jpg">
    <source src="../media/video/performance/百年文脉传承人谈起源.mp4" type="video/mp4">
</video>
```

**关键点**：
- ✅ `controls` - 显示播放控件（已添加）
- ✅ `preload="auto"` - 预加载视频
- ✅ `poster` - 封面图
- ✅ 视频路径正确（无"视频"前缀）

### CSS
```css
.video-wrapper video {
    width: 100%;
    aspect-ratio: 16 / 9;
    display: block;
    background: #000;
    object-fit: contain;
}
```

**关键点**：
- ✅ 宽度100%
- ✅ 16:9比例
- ✅ 黑色背景
- ✅ 内容居中显示

## 🔧 临时解决方案

### 方案1：使用绝对路径测试

如果相对路径有问题，可以暂时使用绝对路径：
```html
<video id="inheritorVideo" preload="auto" controls>
    <source src="/d:/projects/南曲/media/video/performance/百年文脉传承人谈起源.mp4" type="video/mp4">
</video>
```

### 方案2：使用在线视频占位

如果没有本地视频，可以使用在线占位视频测试：
```html
<video id="inheritorVideo" preload="auto" controls>
    <source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4">
</video>
```

### 方案3：移除自动播放，仅手动控制

如果自动播放一直有问题，可以先移除自动播放功能：
```html
<video id="inheritorVideo" preload="auto" controls poster="../images/modules/module1/视频封面-传承人谈起源.jpg">
    <source src="../media/video/performance/百年文脉传承人谈起源.mp4" type="video/mp4">
</video>

<!-- 移除 JavaScript 自动播放代码 -->
```

## 📊 浏览器控制台检查

打开浏览器控制台（F12），应该看到以下日志：

**正常情况**：
```
视频已加载，时长: XXX 秒
视频开始播放
```

**异常情况**：
```
自动播放被阻止: NotAllowedError ...
请手动点击视频播放
```

或：

```
GET http://.../百年文脉传承人谈起源.mp4 404 (Not Found)
视频加载失败
```

## 🎬 测试步骤

1. **打开页面**
   - 双击 `module1/page1.html` 在浏览器中打开

2. **检查视频是否显示**
   - 应该看到视频播放器（黑色区域或封面图）
   - 播放控件应该可见

3. **手动测试播放**
   - 点击播放按钮
   - 确认视频可以正常播放

4. **测试自动播放**
   - 刷新页面
   - 点击页面任意位置
   - 滚动到视频位置
   - 查看控制台日志

## 💡 建议

1. **确保视频文件存在**
   - 检查文件名是否完全一致
   - 注意中文文件名编码问题

2. **简化测试**
   - 先用一个简单的MP4文件测试
   - 确认代码无误后，再使用真实视频

3. **使用本地服务器**
   - 直接双击HTML文件可能有跨域问题
   - 建议使用VS Code的Live Server或其他本地服务器

## 🚀 快速修复

如果视频还是不显示，尝试以下步骤：

1. **检查文件是否存在**
   ```bash
   dir "d:\projects\南曲\media\video\performance"
   ```

2. **使用简化的HTML测试**
   创建一个新文件 `test-video.html`：
   ```html
   <!DOCTYPE html>
   <html>
   <head><title>视频测试</title></head>
   <body>
       <h1>视频测试</h1>
       <video controls width="800">
           <source src="../media/video/performance/百年文脉传承人谈起源.mp4" type="video/mp4">
       </video>
       <p>如果视频不显示，检查文件路径</p>
   </body>
   </html>
   ```

3. **确认文件编码**
   - 确保文件名使用UTF-8编码
   - 避免特殊字符

---

**最后更新**：2026-01-26
